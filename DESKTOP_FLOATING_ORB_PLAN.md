# Daily Plan 系统悬浮球产品与实施方案

> 编写日期：2026-09-10。依据当前仓库代码和 Electron 官方文档。本文是待实施方案，不表示相关功能已经完成或通过实机验证。
>
> 产品定位：把 Daily Plan 从“需要打开的规划网站”延伸成“随时可见、随手可用的桌面任务与专注助手”。小球本身表达今日进度和计时状态；展开后完成任务操作与计时控制；完整网站继续承载规划和复盘。

## 1. 推荐决策

采用 **Electron 桌面外壳 + 现有原生 JavaScript 前端 + FastAPI + SQLite**，先支持 Windows 10/11，保留浏览器入口。

第一版应交付完整闭环：**桌面球 → 靠边缩起 → 展开任务 → 开始专注 → 收起后可见计时状态 → 完成并记录 → 网站同步**。计时持久化和并发写保护必须随第一版可用产品交付，不能留到长期使用之后补做。

推荐三个原生窗口：按需打开的完整主窗口、透明小球窗口、独立紧凑面板窗口。小球与面板共享状态，但分别管理原生尺寸。使用贴近锚点的短过渡，让用户感觉面板从球旁展开。

以下为默认产品选择，可在后续体验验证中调整，不构成实施前的阻塞项：

| 决策 | 默认方案 | 原因 |
| --- | --- | --- |
| 支持平台 | Windows 10/11，先 x64 | 与现有本地启动方式一致，控制窗口兼容性范围 |
| 小球风格 | 黑底、荧光绿、双层信息环 | 延续网站风格，让任务与计时各有明确含义 |
| 面板结构 | 顶层“任务 / 专注”；任务内“今日 / Todo” | 对应现有两类数据，避免误合并 |
| 停靠方向 | 首版左右工作区边缘 | 上下边缘易干扰任务栏、窗口控制区 |
| 默认计时 | 正计时，可关联任务，也可自由专注 | 复用现有心智；倒计时留给后续版本 |
| 休眠、锁屏 | 自动暂停，恢复后手动继续 | 防止无人操作时间变成专注记录 |
| 面板失焦 | 收回；固定面板后保持 | 减少遮挡，同时允许持续查看 |
| 自动启动 | 默认关闭，可设置开启 | 首次安装不改变系统启动习惯 |
| 数据权威 | FastAPI/SQLite | 浏览器和所有桌面窗口使用同一套业务状态 |
| 断连写入 | 保留草稿，禁止假装成功；恢复后重试 | 避免多个 localStorage 副本互相覆盖 |

## 2. 当前项目核查

### 2.1 已有能力与真实差距

| 范围 | 已确认的当前实现 | 本次改造含义 |
| --- | --- | --- |
| 前端 | `index.html`、约 3300 行 `app.js`、按功能拆分的 CSS；没有现成 Electron/package.json | 添加桌面工程，逐步抽共享逻辑，无需整体改写 React |
| 视觉 | `style/variables.css` 的强调色实际是 `#00ff88`，页面背景 `#0f0f0f`，文字 `#e0e0e0` | 以实际 CSS 为准；开发说明中的 `#4ade80` 与代码不完全一致 |
| 每日任务 | `app.js` 的 `addTask`、`toggleTask`、`renderTasks`；有标签、子任务、时间段、延续 | 悬浮“今日”复用字段和排序规则 |
| Todo | 左侧目标数组内 `type: 'todo'`；`toggleGoalItem`、`renderGoals` | 和每日任务分开显示、分开更新；Goal 不被误当成可勾选 Todo |
| 计时器 | `app.js` 的 `timerState`、`timerSyncFromClock`、`timerStart/Pause/Reset` | 已使用时间戳计算，不是单纯每秒加一；缺持久化、任务关联和跨窗口权威状态 |
| 计时记录 | 重置时，至少 60 秒才写入；只有 `id/start/end/duration`，按开始日期归档 | 新版需要明确“结束并保存”，增加绝对时间、暂停分段、关联任务 |
| 写入方式 | `saveTasks`、`saveSessions`、目标保存均发整组 PUT；后端替换 JSON | 多窗口有覆盖风险；学习记录也必须一起解决 |
| 同步方式 | 启动 `/api/data` 加载，然后依赖各页面内存 cache | 需要主动获取变更；当前没有实时共享状态 |
| 降级方式 | 部分写入失败后将缓存写入 localStorage；sessions 无对应降级 | 不能把这个机制直接扩成多窗口离线同步 |
| 服务地址 | `app.js:12` 写死 `http://localhost:8000/api` | 改为同源 `/api`，配合统一实例地址 |
| 启动脚本 | BAT/VBS 监听 `0.0.0.0:8000`，用端口占用判断服务存在 | 桌面模式仅监听回环地址；必须验证服务身份和版本 |
| 数据位置 | `backend/main.py:23` 固定 `backend/data.db` | 安装版迁到用户数据目录，通过配置指定 |
| 静态托管 | `backend/main.py` 将整个项目根目录作为静态目录 | 发布时仅暴露前端资源，数据库和桌面脚本移出静态资源范围 |

代码定位以函数名为主，行号仅对应编写时版本。另参考仓库现有 [时间功能说明](TIME_FEATURE_PLAN.md) 和 [项目说明](README.md)。

### 2.2 对上一份分析的修正

1. **Electron 路线合理，但 8/10 是判断，不是验证结果。** 最有不确定性的部分是透明窗口、鼠标穿透、跨屏缩放和生命周期，不是任务列表本身。
2. **时间戳算法已存在。** 核心工作是将运行状态移到后端，并定义异常恢复规则，不是重新发明计时算法。
3. **“Todo”需要拆清语义。** 今日任务是 `tasks[date]`，左侧 Todo 是 `goals` 内一种类型。直接创建统一 `/api/todos` 而不处理来源会增加歧义。
4. **只新增 PATCH 不够。** 若旧网页继续整组 PUT，仍能把桌面刚修改的数据覆盖掉。两个入口的写路径必须一起升级。
5. **同一个透明窗口动态变尺寸不应直接定案。** 官方明确列出透明窗口不可调整大小的限制；透明像素也不会天然穿透鼠标。因此先采用固定尺寸透明球与独立面板，单窗只作为实机探索分支。[Electron 透明窗口限制](https://www.electronjs.org/docs/latest/tutorial/custom-window-styles)

## 3. 产品范围与核心路径

### 3.1 三层体验

| 层级 | 主要问题 | 显示内容 | 常用动作 |
| --- | --- | --- | --- |
| 边缘柄 / 小球 | 今天做了多少？现在是否计时？ | 今日完成比例、计时运行/暂停、必要错误提示 | 悬停查看、单击展开、拖动、右键 |
| 紧凑面板 | 下一步做什么？如何开始或结束专注？ | 今日任务、Todo、当前计时、今日累计 | 新增、编辑、勾选、任务计时、暂停、结束 |
| 完整网站 | 如何安排和回顾？ | 目标、日历、时间安排、备忘录、历史统计 | 长期规划、批量整理、详细复盘 |

### 3.2 应达到的体验

用户正在其他软件工作：屏幕右侧留着一枚绿色状态柄。悬停后小球露出，显示今日完成 `3/7`。点击后打开任务面板，在“完成接口联调”一行点播放，切换到专注页。用户收起面板，小球中心显示已专注分钟数，内环缓慢表达当前分钟节奏。结束后可“保存本次”或“保存并完成任务”，外环进度更新。用户随后打开网站，看到同一条任务状态和同一条学习记录。

首版中“从小球到开始某条已有任务”不超过三次点击；运行中打开面板后，一次点击即可暂停。

### 3.3 版本边界

| 优先级 | 内容 |
| --- | --- |
| P0：可长期试用 | 系统置顶球、左右停靠、紧凑双页、今日/Todo基本操作、关联正计时、暂停/结束、持久化、同步、托盘、异常恢复、旧数据迁移 |
| P1：完善版 | 子任务紧凑编辑、Todo加入今日、快捷键设置、开机启动、球尺寸设置、专注微时间轴、安装包、导出与备份入口 |
| P2：增强版 | 番茄钟/倒计时、目标时长环、更多统计、自动更新、其他操作系统适配 |

首次内部演示可使用模拟数据先验证窗口动效；任何写入真实数据的试用版必须具备 P0 的持久化、同步和迁移保护。

## 4. 小球视觉与信息表达

### 4.1 视觉规格

以下尺寸单位均为 DIP；在通常的 Electron 100% 页面缩放下与 CSS 像素对应。首版建议固定页面缩放，字体尺寸在产品设置中控制。

| 元素 | 建议规格 | 实现 |
| --- | --- | --- |
| 球体 | 默认直径 56；P1 提供 48/56/64 | 固定原生窗口尺寸配合 SVG/CSS |
| 外环 | 3px；表达今日已完成/今日全部任务 | SVG `stroke-dasharray/offset` |
| 内环 | 1.5px；表达正计时分钟内节奏 | SVG 弧线或旋转标记，不与完成率共用一条环 |
| 核心 | 默认 `3/7`；运行时 `12m`；超过一小时 `1h+` | 精确时长放悬停提示和面板，不在 56px 内堆六位数字 |
| 底色 | `#0f0f0f`，球边缘近黑 | 同网站黑绿风格 |
| 强调色 | `--accent: #00ff88` | 从共享 token 文件读取 |
| 状态辅色 | 警告 `#ffaa00`，失败 `#ff4444` | 仅小角标使用，配文字说明 |
| 字体 | 数字用现有等宽栈；中文用系统中文字体回退 | 离线可读；若打包字体需确认字体许可 |
| 发光 | 很弱的内部渐变，避免大范围外光晕 | 减少透明窗口实际占用范围 |

`variables.css` 当前同时包含 token 和 body 布局，不能直接全部套到小球。新增 `style/tokens.css` 放共享变量；网站原来的 body 规则留在网站样式；浮窗独立设置透明背景、零 padding。

### 4.2 小球状态表

| 状态 | 用户看到什么 | 动效 | 单击行为 |
| --- | --- | --- | --- |
| 今日无任务、未计时 | 中心 `+`，暗色空环 | 静止 | 打开今日列表并聚焦新增 |
| 有任务、未计时 | 中心 `3/7`，外环对应完成率 | 默认静止 | 打开上次任务子页 |
| 正在计时 | 中心 `12m`，外环保持今日进度 | 内环每 60 秒一个周期，核心极弱呼吸 | 打开专注页 |
| 已暂停 | 中心暂停符号；提示累计时长 | 内环冻结，亮度降低 | 打开专注页，主按钮“继续” |
| 完成任务 | 中心短暂勾号 | 外环补齐对应弧段，单次轻亮 | 不改变原交互 |
| 今日全部完成 | `7/7`，完整外环 | 一次完成反馈后静止 | 查看今日已完成 |
| 后端不可用 | 保留上次已确认进度，小型 `!` | 停止表示正常运行的呼吸 | 打开重连状态及恢复动作 |

计时状态决定核心数字；今日任务决定外环；连接状态占独立角标。任务完成反馈不应覆盖断连错误。收到相同操作事件不重复播放完成动画；重启加载历史数据也不播放庆祝动画。

### 4.3 指标含义

- 外环固定统计“今天的每日任务”，不把长期 Todo、子任务重复计入分母；标签筛选不会改变外环。
- 无任务显示“今日未安排”，不能显示成 100% 完成。
- 子任务只影响所属任务行内的小进度；是否完成父任务沿用显式勾选语义。
- 正计时内环是每分钟循环的节奏提示，不叫“本次完成率”，也不表示时间目标。
- 若 P2 增加目标时长或倒计时，专注页明确显示 `已用 / 目标`，才能显示对应百分比。
- 今日累计使用暂停分段后落在今日的实际时长，包含当前尚未结束的有效分段；同一会话不重复累计。

## 5. 停靠、拖动与窗口行为

### 5.1 停靠状态机

```text
自由小球 --拖到左右边缘并释放--> 贴边小球
贴边小球 --指针离开 1.2 秒--> 缩起边缘柄
缩起边缘柄 --悬停 180ms--> 贴边小球
小球 --单击--> 展开面板
展开面板 --Esc / 收起 / 非固定状态失焦--> 原小球状态
任意显示状态 --托盘“隐藏悬浮球”--> 隐藏
隐藏 --托盘“显示悬浮球”--> 恢复并校正位置
```

具体规则：

1. 指针移动超过 6 DIP 才视为拖动；拖动结束不再触发单击展开。球用 Pointer Events 区分点击和拖动，不能整块声明为原生拖动区后再期待普通点击完整触发。
2. 松手时距离当前显示器工作区左/右边缘 ≤20 DIP，吸附到最近一侧。未达到阈值保持自由位置，不强制每次都贴边。
3. 停靠缩起后展示约 `12 × 40` 的可见状态柄，交互区域约 `20 × 56`。绿色填充高度表示今日完成比例，运行状态增加一条缓慢移动亮线。
4. 使用固定球窗口内部裁切/位移形成边缘柄，不把大半个窗口移到相邻显示器。透明空白区另做穿透处理。
5. 悬停只露出球，不抢键盘焦点；点击才展开并激活面板。边缘柄也可直接点击展开。
6. 输入中、拖动中、面板打开、原生菜单打开时停止自动缩起计时。点击“固定”后，失焦不会关闭面板。
7. 面板打开时，球保留为视觉锚点，但暂不提供拖动；先收起面板再拖动，降低首版双窗协调复杂度。
8. 开机或崩溃恢复默认收起展示，恢复上次位置和所选页；不自动弹出上次的大面板。

### 5.2 面板位置计算

建议面板 `352 × 440`，与球间距 8。位于屏幕右侧时向左展开，位于左侧时向右展开；自由球优先朝剩余空间更大的一边展开。面板距离工作区边缘至少 8 DIP。

用 `screen.getDisplayNearestPoint()` / `getDisplayMatching()` 选显示器，以 `display.workArea` 计算而非整个桌面分辨率。处理负坐标，不把多显示器原点假定为 `(0,0)`；窗口位置统一用 DIP，不重复乘 `scaleFactor`。监听显示器移除、工作区和缩放变化，重新夹取坐标。[Electron screen](https://www.electronjs.org/docs/latest/api/screen)

保存 `{ displayId, dockSide, relativeY, freeRelativeX, mode }`，相对坐标相对于工作区。原显示器不存在则回到主屏右侧约 35% 高度。小工作区下缩短面板，列表内部滚动；极小区域允许面板移动锚点，不裁掉底部控制按钮。

### 5.3 原生窗口实现

| 窗口 | 设计 |
| --- | --- |
| 完整主窗口 | 常规窗口，按需创建；关闭隐藏到托盘；用户明确退出才结束应用 |
| 小球窗口 | `frame:false`、`transparent:true`、`resizable:false`、`alwaysOnTop:true`、`skipTaskbar:true`；固定尺寸 |
| 紧凑面板 | 无边框、非透明黑底、置顶、隐藏任务栏图标；固定尺寸为主，避免透明缩放依赖 |

Electron 提供置顶、无框窗口、`showInactive()` 和 `setIgnoreMouseEvents()` 等控制。小球初次出现用不激活方式显示；面板响应用户点击再获得焦点。实际层级与焦点行为要在 Windows 实机验证。[BrowserWindow API](https://www.electronjs.org/docs/latest/api/browser-window)

透明区域不会自动点击穿透。实现时根据鼠标是否处于球/边缘柄有效形状内切换忽略鼠标状态；利用 Windows 支持的转发能力恢复命中检测，必要时由主进程低频检查光标与有效区域。拖动期间禁用穿透切换。**必须验证“忽略鼠标后还能再次进入并点击”以及底层窗口点击正常。** 不保留覆盖半个桌面的透明窗口。[透明窗口说明](https://www.electronjs.org/docs/latest/tutorial/custom-window-styles)

不把面板简单设为主窗口的子窗，避免完整网站隐藏时连带影响悬浮体验。使用独立窗口和主进程控制显示顺序；菜单打开、球与面板之间焦点切换时设置短期收起抑制标记，防止失焦误关。

普通桌面应用上置顶是首版目标；UAC 安全桌面和独占全屏不承诺覆盖。提供托盘“一键隐藏”与可选快捷键，作为演示和全屏工作场景的可靠入口。

## 6. 紧凑面板功能设计

### 6.1 信息布局

```text
┌─────────────────────────────────┐
│ DAILY PLAN         已同步  固定 ↗ × │
│ [ 任务 ] [ 专注 · 12:34 ]         │
├─────────────────────────────────┤
│ 今日 09/10   [今日] [Todo]       │
│ 已完成 3 / 7  ━━━━━━━━━          │
│ + 添加今日任务…                  │
│ □ 完成接口联调        14:00  ▶   │
│ □ 阅读资料             2/4  ▶   │
│ □ 整理问题清单              ▶   │
│ ▸ 已完成 3                       │
├─────────────────────────────────┤
│ 正在专注：完成接口联调     12:34  │
│ 今日累计 1h 20m                  │
└─────────────────────────────────┘
```

顶部固定按钮包含状态、固定、打开完整应用、收起。列表独立滚动，标签页和当前计时条保持可见。`↗` 在完整应用内定位当前任务；`×` 仅收起面板。

### 6.2 今日任务页

| 功能 | 交互细则 | 实现落点 |
| --- | --- | --- |
| 快速新增 | Enter 保存；支持已有 `#标签` 解析；默认加入今天 | 共享 `parseTagsFromText` + 单条新增 API |
| 任务展示 | 复用未完成优先、开始时间、创建时间排序；显示当前时段高亮 | 从 `renderTasks` 提取纯排序/选择器，浮窗独立 DOM |
| 完成/撤销 | 勾选后短动画并移入折叠区；提供 5 秒撤销入口 | PATCH 显式 `done` 值，撤销也是带版本的新操作 |
| 编辑 | 行内修改标题；Enter 提交、Esc 取消；IME 输入期间 Enter 不提交 | 本地草稿与服务端实体分离 |
| 时间与标签 | 首版显示；详情内可编辑；紧凑浮层不超出窗口 | 共享校验函数，简化现有时间拨盘展示 |
| 子任务 | 首版显示完成数及可勾选详情；P1 补新增/重排 | 子任务独立操作接口，或对父记录做带版本 PATCH |
| 开始专注 | 点击行尾播放，关联该任务并切到专注页 | `POST /api/timer/start` |
| 删除 | 放入更多菜单，给撤销；计时中的任务走专门流程 | 服务端软删除，保留记录引用 |
| 当前任务 | 排程高亮与“正在计时”分别标识 | 排程时间不会自动开始计时，也不会改写关联 |

悬浮今日页始终表示本地今天；历史日期浏览留在网站。午夜刷新日期、完成环和列表，新建任务归入新一天；已有计时继续关联原任务，不自动搬家。

### 6.3 Todo 页

显示 `goals` 中 `type='todo'`，保留置顶、优先级、截止日期和已完成折叠分组。支持新增、编辑标题、完成、计时；Goal 类型仍由完整应用管理。

Todo 与今日任务分别计数；不能因为完成了 Todo 就增加今日外环。P1 增加“加入今日”，创建今日任务并存 `sourceGoalId`；这是一份关联副本，默认分别完成，不隐式联动。界面可提供明确的“同时完成来源 Todo”，由服务端事务处理。

### 6.4 专注页

```text
当前任务  完成接口联调          更换

          [细环与分钟刻度]
             00:12:34
               专注中

           [ 暂停 ]
 [结束并保存]     [保存并完成任务]

今日累计 1h 20m   本次暂停 1 次
最近完成：阅读资料 · 25m
```

| 状态/动作 | 规则 |
| --- | --- |
| 尚未开始 | 展示任务选择和“自由专注”；无关联时主按钮“开始专注” |
| 开始 | 请求待确认时显示“正在开始”；后端成功后才成为运行状态 |
| 暂停/继续 | 原会话持续存在，暂停期间不累计；无需先保存再新建 |
| 结束并保存 | 按当前有效时长生成一次记录，回到待开始状态 |
| 保存并完成任务 | 结束会话与完成关联任务在同一个事务内处理 |
| 放弃本次 | 次级菜单；明确说明不保留本次记录；不改变任务状态 |
| 更换任务 | 有活跃会话时弹出紧凑确认：“保存当前并开始新任务”；取消则不改变 |
| 不足 60 秒 | 新版也保留大于零的显式保存记录，展示“少于 1 分钟”；0 秒不生成记录 |
| 已完成任务 | 默认不可直接启动；提供“重新打开并开始”明确动作 |
| 计时中勾选关联任务 | 询问“保存本次并完成”或取消；不能继续挂着已完成任务无提示计时 |
| 删除关联任务 | 先结束并保存或取消；历史记录保留标题快照 |

网页端必须使用相同规则，包括关联任务完成、删除和开始第二个会话的处理。全应用仅允许一条 active 会话，窗口数量不会增加计时器数量。

### 6.5 空状态与错误状态

- 无今日任务：显示“今天还没有安排”，直接可输入，保留“自由专注”。
- Todo 全完成：显示已完成入口，不自动把 Goal 混入列表。
- 请求提交中：只禁用该操作，草稿保持可编辑；防重复点击。
- 保存失败：任务保留“未保存”状态和重试入口；不显示已同步绿点。
- 并发冲突：显示最新值与用户未提交文本，允许重新提交；禁止静默覆盖。
- 服务断开：展示最后确认快照和断连时间；计时数字标记“待核对”，不显示已正常保存。

## 7. 动效与可视化实施规格

### 7.1 动效语义

| 触发 | 表现 | 建议时长 | 实现方法 |
| --- | --- | --- | --- |
| 指针进入球 | 亮度略升，缩放到 1.03 | 120ms | transform/opacity |
| 按下 | 核心缩至 0.96，释放复位 | 80–140ms | CSS 状态类 |
| 靠边吸附 | 球轻移至工作区边界 | 160–200ms | 主进程有限步移动；每步位置可取消 |
| 缩起/露出 | 球内图形移向边缘、柄淡入 | 180ms | 固定窗口内 transform + clip |
| 面板展开 | 从球方向偏移 8px、缩放 0.97→1、渐显 | 180ms | 面板自身动画，transform-origin 对准球 |
| 面板关闭 | 反向位移并淡出 | 140ms | 动画完成后隐藏原生窗口 |
| 标签页切换 | 内容轻移 6px 并交叉淡入 | 140ms | 内容层变换，容器固定高度 |
| 完成任务 | 勾选、标题淡化、行高度收束、外环补齐 | 主过渡 180ms | 小列表使用 FLIP；只对目标行布局变化 |
| 开始计时 | 内环亮起，主按钮转暂停 | 180ms | 状态驱动样式，不重新挂载整个面板 |
| 运行 | 内环一分钟一周；核心弱呼吸 | 周期 60s / 4s | SVG/CSS；暂停时冻结相位 |
| 暂停 | 保留相位，降低强度 | 140ms | animation-play-state + 状态类 |
| 全部完成 | 环与勾号一次轻亮 | 总序列约 400ms | 多段短过渡；不循环、不撒满屏粒子 |
| 保存错误 | 小角标显现、错误文案进入 | 140ms | opacity；不持续红色闪烁 |

这些数值是设计初值，需要实机调参。沿用项目“动效有意义、普通过渡不超过 200ms”的原则，长周期动画只表达计时，不用于装饰。

### 7.2 动画工程约束

1. 动画由 `visualState` 派生：`connection / timer / progress / docking / panel` 分开建模，禁止各处随意加 class 造成相互打架。
2. 每次展开/收起带 generation ID；新动作取消旧动画和旧回调，避免快速点击后把新面板隐藏。
3. 原生窗口先定位并准备好内容，再展示；页面缩放动画不能代替原生窗口位置计算。
4. 不对完整 DOM 每秒重绘；每秒只改计时文本，CSS/SVG承担连续视觉运动。
5. 面板隐藏时停止其环形与数字动画；小球处于柄状态时简化绘制；隐藏所有悬浮入口时暂停装饰绘制。
6. 保持 Chromium 默认后台节流；计时准确性不能靠关闭节流维持。
7. 支持 `prefers-reduced-motion` 和应用内“减少动效”：停靠瞬时或短淡入、取消呼吸和滚动数字，保留状态图标与真实进度。
8. 按钮有可见焦点，计时不每秒向屏幕阅读器播报；只播报开始/暂停/结束。状态不能仅靠颜色区分。

### 7.3 面板可视化演进

首版显示今日完成条、关联任务子任务进度、本次时长与今日累计。P1 增加最多 6 段的最近专注微时间轴：有效工作段为绿、暂停为空隙，悬停显示时间与标题。完整热力图保留在网站，避免在 352px 面板里压缩成不可读图表。

## 8. 应用架构与代码落点

```text
系统托盘 / 原生窗口 / 电源事件
                 │
          Electron 主进程
      窗口位置、IPC、后端进程管理
          │                 │
      小球渲染器          面板/主窗口渲染器
          └──── 共享前端业务模块 ────┐
                                   │ HTTP，同源 /api
外部浏览器 ─── 同一网站业务模块 ──────┤
                                   ▼
                         FastAPI 单一后端
                  命令、版本校验、事务、变更序号
                                   ▼
                       用户目录 SQLite
```

Electron 主进程拥有“窗口状态与系统集成”；FastAPI 拥有“任务状态、计时状态与历史记录”。两个进程不分别维护能互相冲突的业务真相。网页端无需调用 Electron API，也能完成所有业务动作。

### 8.1 建议文件结构

```text
desktop/
  main.cjs                 # 单实例、启动、窗口与退出编排
  preload.cjs              # 受限桌面能力桥
  backend-manager.cjs      # 服务发现、启动、健康检查、停止
  window-manager.cjs       # 球、面板、主窗口
  dock-controller.cjs      # 几何、吸附、恢复、显示器事件
  tray.cjs                 # 托盘菜单
  settings.cjs             # 原子保存窗口偏好
floating/
  orb.html / orb.js
  panel.html / panel.js
shared/
  api.js                   # 请求、错误、requestId
  store.js                 # 快照、revision、订阅
  task-model.js            # 标签、排序、任务来源
  timer-model.js           # 展示计算及状态选择器
style/
  tokens.css
  floating-orb.css
  floating-panel.css
  motion.css
backend/
  main.py
  models.py                # Pydantic 输入/响应
  services/tasks.py
  services/timer.py
  services/changes.py
  migrations/
tests/
  backend/
  desktop/
package.json
```

第一轮共享模块使用明确命名的 IIFE/命名空间，如 `window.DailyPlan`，通过 script 顺序加载，适配现有无构建项目。Electron 主进程使用 `.cjs`，避免和浏览器脚本模式混淆。后续若迁移 ES modules，应统一入口和加载方式，不在现有普通 script 中零散插入 import。

新增 CSS 规则放各自模块；`style.css` 继续只负责 import。浮窗有独立样式入口，只加载 token 与自己需要的组件，避免网站全局布局污染。

### 8.2 复用与重写边界

| 当前模块 | 处理 |
| --- | --- |
| 日期、标签解析、排序、格式化 | 提取共享纯函数 |
| `cache`、`apiPut`、`saveTasks/saveSessions` | 替换为命令 API + 有 revision 的 store |
| `renderTasks/renderGoals` | 网站 DOM 保留并接新 store；浮窗重新写紧凑渲染 |
| `timerState` | 不再作为业务权威；只保留服务端快照与显示锚点 |
| `timerUpdateRing` 等视觉函数 | 提取可接受元素引用的绘制函数，去掉固定 ID 依赖 |
| 现有 timer 拖动逻辑 | 仅限网页 DOM；系统窗口另写主进程位置控制 |
| `recordTimerSession` | 改为后端结束命令生成记录，禁止前端自行拼接整个 sessions 数组 |
| `loadFromBackend` 与 localStorage 迁移 | 改为版本化 bootstrap 与显式导入流程 |

## 9. 数据模型、接口与多端同步

### 9.1 第一版的数据策略

为减少一次性迁移范围，第一版可继续保存每日任务 JSON 桶和目标 JSON 桶，**但所有写入通过后端记录级服务完成**：

- `BEGIN IMMEDIATE` 开启写事务，读取最新桶，定位单条，检查该实体版本，修改后提交。
- 每条任务/目标增加 `version`、`updatedAt`；使用显式值更新，如 `done:true`，不使用可重复翻转的 toggle 命令。
- 子任务修改也经服务端定位，不能让前端提交陈旧的整个父任务覆盖。
- 后端维护 tombstone 或等效删除记录；撤销通过版本化恢复执行。
- 任务 identity 首版使用 `{kind:'task', dateKey, id}` 或 `{kind:'todo', id}`，避免直接假设所有历史 ID 全局唯一。
- 批量延续任务需要专门事务命令，同时修改源日期与目标日期；保留既有产品语义并验证计时引用。

任务量增大、查询需求复杂时再将任务迁为逐行表；这个迁移不应阻塞首版浮窗。计时与幂等操作记录从首版就用逐行表，因为需要强事务与关联约束。

### 9.2 建议新增表

| 表 | 关键内容 |
| --- | --- |
| `entity_versions`（如版本未写入 JSON） | entityKey、version、updatedAt |
| `timer_state` | 单例行；sessionId、status、revision、accumulatedMs、runningSinceUtc、checkpointAt、checkpointElapsedMs、ownerEpoch、pauseReason |
| `focus_sessions` | id、关联 kind/date/id、titleSnapshot、startedAtUtc、endedAtUtc、durationMs、timezone、status、legacy 标识 |
| `focus_segments` | sessionId、startUtc、endUtc、durationMs；每次暂停关闭一段 |
| `operations` | requestId 唯一、命令摘要、结果 JSON；重复请求返回原结果 |
| `change_log` | 单调 seq、实体类型与 key、entityVersion、operationId |
| `schema_migrations` | 迁移版本、时间、结果 |

全局至多一条活跃会话，由 `timer_state` 单例和事务服务强制执行。输入通过 Pydantic 校验；时间保存为 UTC 绝对时间，界面按明确的本地时区展示。

### 9.3 API 草案

以下均为待新增或改造接口，不是当前已存在接口。

| 方法与路径 | 用途与关键约束 |
| --- | --- |
| `GET /api/health` | appId、apiVersion、schemaVersion、instanceId；不返回私人任务 |
| `GET /api/bootstrap` | 一致性快照、全局 revision、serverNow、今日任务、Todo摘要、当前计时 |
| `GET /api/tasks?date=YYYY-MM-DD` | 某日完整记录与版本 |
| `POST /api/tasks` | 新建单条每日任务，带 requestId |
| `PATCH /api/tasks/{date}/{id}` | `expectedVersion` + 允许字段 + requestId |
| `DELETE /api/tasks/{date}/{id}` | 带版本和幂等标识，处理关联计时保护 |
| `POST /api/tasks/carry-over` | 源日期、目标日期、记录 ID 与版本；跨桶事务 |
| `POST /api/goals` | 创建 Goal 或 Todo，明确 type |
| `PATCH /api/goals/{id}` | 单条更新；由服务层约束类型行为 |
| `DELETE /api/goals/{id}` | 单条删除与备忘录/计时引用处理 |
| `GET /api/timer` | 当前权威状态、revision、elapsedMs、serverNow |
| `POST /api/timer/start` | 关联可空；已有 active 则冲突，不暗中启动第二个 |
| `POST /api/timer/pause` | sessionId、expectedRevision、requestId |
| `POST /api/timer/resume` | 同一 session 恢复 |
| `POST /api/timer/finish` | 保存；可带 completeTarget=true，事务内完成关联任务 |
| `POST /api/timer/switch` | 原子保存当前并开启新关联会话 |
| `POST /api/timer/discard` | 丢弃当前会话，保留必要操作日志 |
| `GET /api/sessions?date=...` | 每日分段统计与历史记录 |
| `DELETE /api/sessions/{id}` | 单条删除历史，不再整天替换 |
| `GET /api/changes?after=seq` | 变更实体与当前序号；过期游标返回需要重取快照 |

工作标记和备忘录也纳入变更序号；整段文本写入使用版本冲突检查，避免两个网页覆盖备忘录。`409` 返回冲突原因和最新版本；`422` 返回输入问题；命令请求应返回服务端确认后的实体。

```json
{
  "requestId": "唯一操作ID",
  "expectedVersion": 4,
  "patch": { "done": true }
}
```

同一 requestId 只能对应同一命令内容；重复标识携带不同内容应拒绝。数据库提交与 operations/change_log 写入必须在同一事务中，解决“保存已成功但响应丢失”的重复提交。

### 9.4 同步方案

第一版采用**增量轮询 + 操作后立即刷新 + 窗口恢复时校验**，无需先搭 WebSocket：

1. 页面启动读取一致性 bootstrap，记录快照 seq。
2. 面板或主窗口可见时每 1 秒取 changes；仅小球存在时每 2 秒取一次；业务响应可经 IPC 通知同进程其他窗口提前刷新。
3. 同一 Electron 内由一个同步协调器合并后台请求；普通浏览器独立轮询。隐藏网页降低频率，重新可见时先拉最新状态。
4. 发现实体变化按 key 获取最新值；忽略比当前版本更旧的响应，不用陈旧响应覆盖新操作。
5. 草稿独立保存。后台刷新不能重建正在输入的表单，也不能把光标移走。
6. 写命令使用服务端返回值更新 store。可撤销任务操作允许乐观反馈；计时状态转换以服务端确认为准。
7. 游标缺口、服务重启或版本不匹配时重取快照。每 60 秒做一次 revision 校验，防止长期漂移。

目标是外部网页与悬浮球的变更正常情况下 2 秒内可见；同应用用户操作通常更快。这是验收目标，尚无当前性能数据。P1 如需更低延迟，可让持久 change_log 支撑 SSE；不要把 BroadcastChannel 当作跨浏览器同步机制。

### 9.5 旧写接口与离线行为

升级必须同时替换网站、悬浮面板和计时记录的写路径。旧整组 PUT 不再接受无版本覆盖：旧客户端写入返回明确的升级提示；不要留下一个可绕过新服务的兼容写入口。新 bootstrap 提供最低客户端版本，打开的旧标签页需刷新。

P0 不做跨端离线自动合并。后端断开时保留最后快照和输入草稿，写入显示待重试；恢复后用原 requestId 查询或重试，冲突则人工选择。正在运行的计时可展示估算，但标明未确认，恢复时以服务端恢复状态重新校准。

localStorage 的旧数据只作为导入源。首次启动检测到旧来源时提供记录数量预览和导入结果，不再把“数据库非空”当成无需处理本地未同步数据的理由。未来离线队列必须记录单条命令、版本和幂等键，不能排队保存整个 cache。

## 10. 计时可靠性与恢复

### 10.1 状态机

```text
idle --start--> running --pause--> paused --resume--> running
running/paused --finish--> idle + 一条完成记录
running/paused --discard--> idle
running --锁屏/休眠/正常退出--> paused（带原因）
running --后端异常中断后恢复--> paused（截至最后可信检查点）
```

窗口隐藏、网页刷新或渲染器崩溃不修改业务状态。后端仍运行则继续计时，重建界面后从 API 恢复。

### 10.2 时间计算

后端存储每段开始的 UTC 时间以便恢复与展示；活进程内使用单调时钟计算持续时长，避免用户调整系统时间导致倒退。基础关系为：

```text
有效累计 = 已关闭分段累计 + 当前运行分段的单调时钟差值
暂停状态 = 已累计，不继续增加
```

API 返回 `elapsedMs + serverNow + status + revision`。前端接收时建立 `performance.now()` 显示锚点，在两次同步间平滑推算；每秒 interval 只触发绘制，不累计权威秒数。后端恢复时不能重用上一进程单调时钟起点，使用 ownerEpoch 区分进程世代。

运行中每 5 秒写一次检查点；开始、暂停、恢复、结束立即事务提交。后端重启发现旧 running 状态时，自动恢复为暂停并以最后可信累计为准，提示“上次运行中断，已保留至最近检查点”。此策略最多可能丢失约一个检查点周期，不把停机数小时默默计入专注。

### 10.3 睡眠、锁屏、退出

Electron 的 `powerMonitor` 提供 suspend/resume、Windows/macOS 的 lock-screen/unlock-screen 等事件，可用于发出系统暂停命令；事件触发不代表一定来得及落盘，所以仍需检查点恢复。[powerMonitor API](https://www.electronjs.org/docs/latest/api/power-monitor)

| 情况 | 明确行为 |
| --- | --- |
| 收起面板、关闭完整窗口 | 继续计时，托盘与小球仍可查看 |
| 用户锁屏或系统休眠 | 主进程请求暂停；恢复只提示继续，不自动重启 |
| 正常退出应用 | 暂停并等待确认后关闭自有后端；下次启动可继续原会话 |
| 系统关机来不及处理 | 下次按检查点恢复为暂停 |
| 仅渲染器崩溃 | 后端继续计时，重新加载恢复 |
| 后端崩溃 | 进入重连，恢复到检查点并暂停，说明缺口 |
| 系统时间跳变 | 持续时长用单调时钟保护；结束/开始时间轴标记异常并允许后续校正 |
| 浏览器独立模式 | 后端检查心跳/时钟间隙；休眠恢复发现异常间隔时保守暂停并提示核对，不能假设有 Electron 电源事件 |

后端运行循环也记录上次可信 tick，比较壁钟与单调时钟间隙，补偿系统事件丢失。检测到长空档时不能自动把空档全部算成工作时间。

### 10.4 跨日与历史兼容

会话保留绝对开始/结束时间和时区，每次暂停结束一个有效分段。跨午夜时，统计层按当地午夜边界切分分段用于每日汇总，原会话仍只有一条。例如 23:50 至次日 00:10 无暂停，前日与次日各计 10 分钟。

旧数据只有 HH:MM 和累计时长，无法还原暂停分布。导入时保留原日期、原 start/end/duration，标记 `legacy=true`；旧记录继续按旧日期归档，不伪造精确分段或自动推断跨日。新旧统计展示注明旧记录的时间分布精度有限。

结束会话、汇总分段、完成关联任务、写幂等结果、发布变更序号在同一事务内。所有时长基于同一分段来源，避免列表总计与热力图分别计算出现差异。

## 11. 桌面生命周期、服务与安装

### 11.1 单实例与服务发现

Electron 使用 `requestSingleInstanceLock()` 防止重复启动；第二次启动仅唤起既有窗口。窗口位置与偏好放 `app.getPath('userData')` 下，正常应用生命周期与开机启动设置使用 Electron app 能力。[Electron app](https://www.electronjs.org/docs/latest/api/app)

建议流程：

1. 获取桌面单实例锁，读取用户数据路径和服务描述文件。
2. 若已有服务，校验 appId、apiVersion、schemaVersion、实例标识及认证，不以“端口正在监听”判断它属于 Daily Plan。
3. 不存在则启动自己的 Python 后端，仅监听 `127.0.0.1`。建议由后端绑定可用端口并通过就绪消息返回端口，避免先探测后绑定的竞争。
4. 健康检查成功才允许真实数据写入；超时展示本地启动错误页和重试入口。
5. 桌面主窗口和“在浏览器打开”均使用同一个实际服务 origin；`API_BASE` 改为相对 `/api`。
6. 服务描述记录实例身份与地址，不记录到仓库。后端还需用户目录级单写进程锁，避免两个不同端口同时操作同一数据文件。

浏览器独立启动脚本也改用相同服务发现流程；旧 8000 服务若协议不兼容，明确提示停止旧实例后升级，不在后台启动第二套数据库让用户误以为已经同步。

### 11.2 托盘与退出

托盘菜单：显示/隐藏悬浮球、打开完整应用、暂停/继续、设置、退出。暂停/继续必须展示当前状态；长标题截断并保留 tooltip。菜单点击调用同一后端计时命令。Electron Tray 提供原生托盘图标与菜单能力。[Tray API](https://www.electronjs.org/docs/latest/api/tray)

主进程持有托盘引用。面板关闭只收起；完整窗口关闭隐藏；`退出` 设置 quitting 标记后进行有时限的保存与停机。只停止本应用启动的后端；若附着于独立服务，退出桌面不杀该服务。服务仍被外部浏览器使用而桌面准备停止时，展示明确退出提示或提供“仅退出桌面、保留服务”。

后端异常建议按 1、2、5 秒间隔尝试恢复，连续失败后停止自动重启并显示按钮，避免无限进程循环。子进程标准输出写滚动日志，不把私人任务文本和认证令牌写入日志。桌面退出时通过退出协议和父进程存活检查清理自有服务，防止异常退出产生孤儿进程。

### 11.3 本地服务与桌面边界

这是落地架构的一部分：

- 只监听回环地址；移除当前任意来源 CORS。限制 Host/Origin 和写请求类型，生产只允许应用自身来源。
- 桌面启动生成随机实例凭据，渲染器不能访问任意系统能力。浏览器通过一次性本地启动授权交换 HttpOnly、SameSite 会话 Cookie；令牌不长期放 URL/localStorage，交换后清理地址。
- 桌面渲染器启用 `contextIsolation`、sandbox，关闭 `nodeIntegration`；preload 仅暴露窗口展开、收起、设置等白名单方法，主进程校验来源与参数。
- 限制导航、新窗口与外链协议；任务标题用文本节点渲染。设置匹配现有前端的 CSP，不能为方便直接关闭 webSecurity。
- 发布资源采用白名单目录；`backend/data.db`、WAL、配置、桌面主进程脚本不放在静态托管根下。

上述隔离与 IPC 校验采用 Electron 官方安全建议；认证和同源会话流程是本项目拟定方案，需在实现时测试。[Electron Security](https://www.electronjs.org/docs/latest/tutorial/security)

### 11.4 数据迁移与备份

安装版数据库放用户目录，例如 `%APPDATA%/Daily Plan/data/data.db`，通过 `DAILY_PLAN_DATA_DIR` 注入后端。代码目录、安装目录与数据目录分离；升级或卸载程序默认保留用户数据。

迁移步骤：

1. 识别当前 `backend/data.db` 和可用 localStorage 导出来源，展示来源与条目数量；不预读用户任务正文到日志。
2. 在禁止旧进程继续写入的维护窗口执行一致性备份；活库备份使用 SQLite backup API，不能只复制 WAL 模式下的 `.db` 单文件。[SQLite 在线备份](https://www.sqlite.org/backup.html)
3. 在目标副本上执行版本化迁移；保留旧任务 ID、日期、未知兼容字段和旧记录精度标记。
4. 校验各日期任务数、Todo数、备忘录数、旧会话数及总 duration；抽样核对中文与子任务结构。
5. 切换启动配置到新库后，旧库保留只读备份；不要让新旧脚本分别写两份数据。
6. 回滚前停止写入。若新库已有新数据，先导出新数据再回退，不能直接恢复旧库造成静默丢失。

### 11.5 打包与分发

开发期用明确配置的 Python 环境启动 Uvicorn；安装版打包 Python 后端，用户无需自行 pip 安装。建议先用 PyInstaller onedir，便于诊断依赖和资源路径；它会将解释器与依赖一起分发，仍需在目标 Windows 环境构建与测试。[PyInstaller 工作方式](https://pyinstaller.org/en/stable/operating-mode.html)

Electron 构建工具在实施时选定一个并锁定版本，例如 Electron Forge；将 Python 后端作为额外资源复制到安装产物，不能要求它从 asar 内直接作为可执行程序运行。先交付便携测试包，再交付安装包。

安装包验收包含：无 Python/Node 的干净 Windows 用户环境、中文用户名、非管理员安装、路径带空格、升级保留数据、卸载保留数据、托盘图标清晰、服务未弹出控制台。自动更新、签名和发布渠道作为单独 P2 工作，不把“能打包”误写成“已具备可靠自动更新”。

## 12. 分阶段实施与交付

以下工期为单名熟悉本项目的开发者的工程估算，不是承诺；包含基本测试与修复，不包含签名申请、商店审核和其他系统适配。窗口兼容问题较多时应单独增加缓冲。

| 阶段 | 工作与文件范围 | 交付物 | 完成条件 | 估算 |
| --- | --- | --- | --- | --- |
| A：原生能力验证 | desktop 临时骨架；模拟球和面板 | Windows 可运行窗口原型、兼容问题清单 | 置顶、穿透恢复、点击拖动、停靠、跨屏、双窗切换均可演示 | 2–3 人日 |
| B：业务可靠性 | 后端 services/models/migrations；网站 API/store | 持久计时、记录级命令、版本化同步、迁移脚本 | 双网页并发不覆盖；刷新/后端重启有明确恢复；旧 PUT 无法绕过 | 4–6 人日 |
| C：产品主链路 | floating、shared、token 与动效 CSS | 今日/Todo/专注面板及数据驱动小球 | 操作闭环贯通，网站/球/面板状态一致 | 4–6 人日 |
| D：桌面常驻 | backend-manager、dock、tray、settings | 可靠常驻试用版 | 关窗继续、退出暂停、恢复位置、锁屏休眠、断连可恢复 | 3–4 人日 |
| E：分发与验收 | 构建配置、图标、回归与性能检查 | 便携包/安装包、迁移说明、测试报告 | 干净机器安装并通过核心回归，无已知数据丢失问题 | 3–5 人日 |

预计首个可长期试用版本约 **16–24 人日**。A 阶段完成后依据实机结果更新估算。P1 的功能增强可再安排约 4–7 人日；P2 另行评估。

阶段 A 不接入真实写入；阶段 B 完成之前不发布多窗口可编辑版本。第一件值得实现的工作是透明球与面板的真实 Windows 技术验证，同时确认数据改造契约；不要先花数天精修一个尚不具备桌面窗口行为的网页球。

## 13. 验收标准与测试矩阵

### 13.1 产品验收

| 编号 | 场景 | 通过标准 |
| --- | --- | --- |
| UX-01 | 在编辑器、浏览器等常规窗口间切换 | 球保持可见，未交互时不抢焦点 |
| UX-02 | 拖动并靠左右边缘 | 达阈值吸附，离开后缩柄，悬停露出，单击展开 |
| UX-03 | 透明窗口空白区域 | 底层应用可正常点击，重新进入球后可再次点击 |
| UX-04 | 快速连续展开收起 20 次 | 无残影、残留空白窗口、错误焦点、最后状态一致 |
| UX-05 | 面板输入中文、切页、外部更新 | IME 正常，不吞 Enter，不丢草稿，不乱跳光标 |
| UX-06 | 任务完成、暂停、全部完成 | 动效对应真实状态；刷新不重复庆祝 |
| UX-07 | 减少动效、键盘操作 | 状态仍明确，Tab/Enter/Esc 可完成核心流程 |

### 13.2 数据与计时验收

| 编号 | 场景 | 通过标准 |
| --- | --- | --- |
| DATA-01 | 网页改标题同时面板完成另一条任务 | 两条修改均保留 |
| DATA-02 | 两端同时改同一记录 | 一方成功，另一方收到冲突及最新值，不静默覆盖 |
| DATA-03 | 服务提交后客户端未收到响应，重试 finish | 只有一条记录，任务完成一次，返回原操作结果 |
| DATA-04 | 活跃任务在另一窗口被删除/完成 | 服务端统一阻止或走结束计时事务，不留悬空运行 |
| DATA-05 | 旧网页 PUT 和旧迁移入口 | 无法覆盖新版数据，明确要求刷新/导入 |
| TIME-01 | 计时 10 分钟，隐藏窗口、切应用、刷新渲染器 | 正常运行的显示误差目标 ≤1 秒，无重复会话 |
| TIME-02 | 暂停 5 分钟再恢复 | 暂停时长不计入总时长与时间轴 |
| TIME-03 | 强制结束后端，重新启动 | 恢复为暂停，显示中断原因；损失上限目标为最后检查点后约 5 秒 |
| TIME-04 | 锁屏/休眠 10 分钟后恢复 | 不把这 10 分钟算作专注；不自动恢复运行 |
| TIME-05 | 23:50→00:10，无暂停 | 两日各计 10 分钟，总计 20 分钟，只有一条会话 |
| TIME-06 | 调整系统时间前进/后退 | 有效时长不倒退、不凭空暴增，异常时间轴有标记 |
| MIG-01 | 使用旧数据库副本迁移 | 数量与总时长校验通过，旧数据和回滚副本保留 |

### 13.3 桌面与性能验收

测试 Windows 10/11、100%/125%/150%/200% 缩放、混合 DPI 双屏、左侧负坐标副屏、任务栏四边与自动隐藏、拔插显示器、休眠唤醒、中文路径、端口占用、无网络、后端启动失败。

建议在指定基准机器记录下列指标，作为目标而非当前已实现的性能：

- 面板已有进程时点击到可交互目标 ≤250ms；冷启动到球可操作目标 ≤5 秒。
- 可见面板同步目标 ≤2 秒；恢复窗口先校验数据再允许依赖当前状态的操作。
- 空闲球状态总 CPU 平均目标 <1%；运行计时状态平均目标 <2%，以任务管理器整机口径注明机器配置和采样方式。
- 球与后端常驻时总内存先设预算 300MB；打开完整主窗口单独记录，不把多进程内存遗漏。超预算先查隐藏窗口、全量 cache 和动画。
- 连续运行 8 小时后无持续内存增长、重复 interval、重复后端进程或明显窗口闪烁。
- 动画目标接近 60fps；出现性能问题优先降低发光和循环动画，任务操作与计时展示保持正确。

自动化重点覆盖后端事务、版本冲突、幂等计时、跨日分段、迁移；前端纯函数测试排序、显示选择器和停靠几何；Electron 集成测试覆盖窗口创建和主链路。鼠标穿透、系统托盘、UAC/全屏边界、DPI 与休眠仍需要真实 Windows 手工测试，不能仅用网页截图判定通过。

## 14. 风险与处理优先级

| 风险 | 影响 | 处理与退路 |
| --- | --- | --- |
| 透明窗口缩放或闪黑 | 破坏展开体验 | 固定球窗口 + 非透明面板；先通过阶段 A 再精修动画 |
| 穿透切换失效 | 球点不到或挡住底层应用 | 独立验收命中恢复；必要时退为更小的实色圆角柄，透明球问题修复后再启用 |
| 双窗失焦竞争 | 面板一打开就关闭 | 菜单/内部操作抑制收起，显式窗口状态机和取消标识 |
| 旧整组 PUT 存续 | 多端丢数据 | 新旧写入口统一切换，旧写入明确拒绝 |
| 两套数据库并行运行 | 用户看见数据不一致 | 统一 userData 路径、服务发现与后端单实例锁 |
| 崩溃后墙钟直接补时 | 误记数小时 | 单调时钟、检查点、恢复为暂停 |
| 现有全局 JS 与 CSS 耦合 | 小功能牵动整个网页 | 提取最小共享服务与 token，浮窗独立渲染 |
| 自动离线回写 | 陈旧数据覆盖新数据 | 首版仅保留草稿与幂等重试，不同步整份缓存 |
| 效果过多或耗电 | 用户关闭常驻入口 | 状态驱动、隐藏停绘、低动效模式和常驻资源预算 |
| 打包依赖或路径问题 | 用户无法直接安装使用 | onedir 后端、明确资源路径、干净 Windows 安装验证 |

## 15. 可直接用于开发排期的清单

- [ ] A1：实现模拟数据的透明球和非透明面板，测定窗口命中与焦点行为。
- [ ] A2：实现左右停靠几何、固定球窗口内缩柄、跨屏位置恢复。
- [ ] B1：定义 bootstrap、命令响应、实体版本、requestId 和变更序号契约。
- [ ] B2：后端任务/目标/子任务/延续操作进入事务服务，替换旧网页写路径。
- [ ] B3：新增计时状态、会话与分段表，完成 start/pause/resume/finish/switch。
- [ ] B4：实现检查点、幂等响应、关联任务保护、跨日统计和中断恢复。
- [ ] B5：升级导入与迁移流程，校验旧任务和旧学习记录。
- [ ] C1：提取 token、API/store、标签/排序/时间显示共享模块。
- [ ] C2：实现今日/Todo 双任务来源、快速编辑与专注页。
- [ ] C3：实现球状态映射、任务完成环、计时环、动效取消和低动效模式。
- [ ] C4：实现增量同步、草稿保护、冲突反馈和断连状态。
- [ ] D1：实现统一服务启动发现、用户目录数据、实例锁与认证。
- [ ] D2：实现托盘、正常退出、电源事件、后端恢复、位置偏好。
- [ ] E1：完成真实 Windows 测试矩阵和 8 小时常驻检查。
- [ ] E2：打包后端与 Electron，完成干净机器安装、升级与数据保留测试。
- [ ] E3：输出版本说明、实际性能数据、已知限制和备份恢复步骤。

最终发布门槛：用户在真实桌面上可连续完成“查看任务—启动专注—靠边收起—暂停恢复—结束保存—网站核对”，且多窗口并发、刷新、休眠、重启均不会静默丢失或重复记录数据。
