# 悬浮界面前端交接 · S3 / S4

更新时间：2026-09-11

本轮已实现前端设计、任务/计时交互，以及实际动效所需的最小原生桥。后端协议仍以 `DESKTOP_BACKEND_HANDOFF.md` 为准。原网站的布局和交互未在本轮修改。

## 体验与范围

- 黑绿专注仪表：深墨绿底、柔绿操作色，外环表示今日任务完成率，内环表示当前一分钟的计时进度。
- 悬浮球：运行显示秒/分钟/小时，暂停冻结，完成任务短暂勾选反馈，断线标记；72 DIP 固定透明窗口。
- 面板：380 × 570 DIP，任务/专注滑动切换，圆角容器，展开收起动画。
- 今日任务与 Todo 分开显示；新增支持 `#标签`；点击标题编辑，Enter 保存、Esc 取消；点击方框完成/恢复，右侧播放按钮开始专注。
- 保留优先级、置顶、截止日排序与时间、标签、子任务进度信息；已完成分组折叠；任务列表可滚动，不再截断为六项。
- 专注支持自由开始、关联任务开始、暂停、继续、结束并保存、确认后放弃。今日统计使用真实 sessions；结束后写入原有每日记录。
- 正在计时的关联任务需要先暂停或结束才能完成，避免误操作；不自动完成任务。
- 右上角固定面板、打开网站、收起。未固定时点击其他应用后收起；输入与编辑稿留在窗口内。
- 左右边缘 20 DIP 内释放吸附；离开 1.2 秒缩起，悬停 180 毫秒展开；保存位置到 Electron userData 下的 `orb-position.json`。
- 减少动态效果设置受尊重；隐藏窗口停止视觉刷新，后台保留同步与计时检查点。

## 文件与关键约束

| 文件 | 职责 |
| --- | --- |
| `floating/panel.html` / `panel.css` | 面板结构、视觉、视图切换与反馈动效 |
| `floating/panel.js` | 任务、编辑稿、乐观更新、计时操作、错误处理 |
| `floating/orb.html` / `orb.css` / `orb.js` | 进度球、分钟环、状态反馈、拖动输入 |
| `floating/clock.js` | 浮窗共用单调时钟，保留亚秒相位 |
| `desktop/main.cjs` | 两个独立原生窗口、开关动画缓冲、吸附、鼠标命中、位置恢复 |
| `desktop/preload.cjs` | 受限 IPC；新增 `onPanelState` 与 `onOrbState` 状态订阅 |
| `desktop/smoke.cjs` | 隔离 fixture 的交互测试与截图 |
| `desktop/native-smoke.cjs` | 原生窗口开关、吸附与恢复检查，独立位置配置 |
| `tests/floating-clock.test.cjs` | 计时相位与状态切换回归 |

不要删除以下机制：

1. `FocusClock` 同一 running 会话只向前校准，保留本地小数秒；不能每两秒把秒表重新从整数锚点启动。
2. `epoch` 丢弃写操作之前发出的迟到 GET，`polling` 防止读请求重叠。
3. 写失败回滚；409 刷新后让用户确认重试，不自动重放整个数据桶。
4. Todo 保存始终带完整 goals 数组，保留 Goal。
5. 轮询内容相同时不重建列表；编辑期间不替换输入 DOM。
6. BroadcastChannel 仅发送刷新/完成提示，数据库快照仍是数据来源。
7. 收起延迟 170ms；重新打开清除旧 hide 定时器，避免第二次打开又被旧回调关掉。
8. 透明区鼠标穿透由主进程每 60ms 查询实际指针并恢复命中；拖动时不穿透，不依赖渲染器收到 mouseenter 才恢复。

## 验证

```powershell
node --test tests/floating-clock.test.cjs
python -m pytest tests/test_s2_backend.py -q
npm.cmd run desktop:smoke
.\node_modules\.bin\electron.cmd desktop/native-smoke.cjs
```

本轮结果：时钟 2 项通过；后端 11 项通过；Electron 交互与截图检查通过；原生窗口检查通过。

交互检查覆盖：完成/恢复、改名、Todo 保留 Goal、开始/暂停/继续、结束写入记录、放弃确认、迟到 GET 不覆盖写入、409 保留新增输入。截图已实际查看并修正计时页底部间距。

原生检查覆盖连续三轮开关、动画反转、左侧吸附、缩起与悬停恢复。该检查模拟屏幕指针坐标，仍需人工体验真实鼠标拖动手感。

截图：`output/playwright/floating-redesign/`（均为隔离测试数据，不是用户的实际记录）。

## 使用与下一步

已运行的 Electron 不会自动加载改动。请从托盘退出 Daily Plan 后，在项目根目录运行 `npm.cmd run desktop`。后端继续按现有方式启动。

实机重点体验：连续展开收起、左右拖动停靠、输入中文、任务完成反馈、长任务列表、暂停/继续及结束后网页每日记录。多显示器/不同 DPI 的真实切换尚未人工验收。

本轮位置恢复为简单绝对坐标加屏幕范围校正；未增加复杂 displayId/相对坐标方案。S5 的自动启动后端、休眠/锁屏策略、安装包和退出协调仍留给后续阶段。窗口开关采用短定时缓冲而非完整动画完成握手；满足当前轻量动画，后续若扩展过渡时长需同步调整。

## 一键启动与开机自启（2026-09-11）

已将桌面“每日规划”快捷方式更新为 `pythonw.exe desktop/launch.py`，并在当前用户的 Windows Startup 文件夹安装 `Daily Plan.lnk`（带 `--autostart`）。登录后静默启动后端与悬浮球，不自动打开网页。手动双击同一桌面图标也可启动。

启动器使用绝对 Python 路径、实际 appId/apiVersion 健康检查和最长 30 秒等待；复用已运行后端，Electron 单实例复用桌面窗口；启动文件锁防止同时点击重复拉起后端。启动日志在 `.tmp/launcher/`。旧桌面快捷方式备份为 `.tmp/launcher/original-desktop-shortcut.lnk`。

取消自启：Win+R 输入 `shell:startup`，删除其中的 `Daily Plan` 快捷方式即可，桌面快捷方式继续可用。项目路径或 Python 安装路径改变后，需要重新运行 `desktop/install-shortcuts.ps1`。

启动器测试：`python -m pytest tests/test_desktop_launcher.py -q`，4 项通过（后端复用、冷启动等待、启动失败、识别其他服务）。这只补齐启动入口，自启不代表完整 S5 已完成；睡眠/锁屏暂停、退出时计时协调仍沿用之前的限制。托盘退出只关闭桌面应用，本地后端保持运行，以便网页继续使用。

## 任务完成交互修复（2026-09-11）

任务完成后会自动展开“已完成”分组，并使用位置差值动画把任务行移动到该分组；目标落到列表可见范围，随后播放绿色完成反馈。恢复任务时会回到未完成区。关联任务仍在运行时，点击完成会先持久化暂停计时，再完成任务；暂停失败时不会错误完成。`desktop/smoke.cjs` 已覆盖普通完成/恢复、完成区自动展开、完成状态落库、动画存在，以及运行中关联任务自动暂停后完成。
