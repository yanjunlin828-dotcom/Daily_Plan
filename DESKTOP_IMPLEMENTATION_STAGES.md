# Daily Plan 悬浮球：个人自用版 AI 分阶段实施计划

> 创建日期：2026-09-10。
> 目标：在现有项目上做出自己每天愿意使用的黑绿桌面悬浮球，轻量、方便、有状态动效。
> 本文负责执行顺序与范围；视觉和交互细节参考 [完整产品方案](DESKTOP_FLOATING_ORB_PLAN.md)，不重复展开。

## 1. AI 开始前先读这里

**本文是个人自用版的执行依据。与原方案范围不一致时，以本文的简化决定为准。** 原方案提供设计参考，不代表必须完成其中全部接口、数据表、安装流程和测试矩阵。

工作方式：

1. 先检查实际代码、项目说明和未提交修改，不覆盖用户已有工作。原方案的行号可能过时，以函数名和实际实现为准。
2. 读取下方进度表和最近的交接记录，确认当前阶段。不要每次从头搭架构。
3. 用户指定阶段时只做该阶段；用户说“继续”时，默认完成下一个未完成阶段。明确要求连续完成全部时，按顺序推进。
4. 每阶段先实现最小可用路径，再补必要的错误处理，最后验证。不要只提交空壳、TODO 或模拟数据并声称完成真实功能。
5. 阶段通过验收后更新本文的状态和交接记录。阶段之间不需要反复询问技术选型。
6. 有合理默认值就直接采用；确实缺少无法推断的信息时再询问。不要擅自加入与当前阶段无关的产品功能。
7. 只为关键数据行为写必要测试；低风险样式改动做实际查看即可。需要安装依赖、启动窗口时按当前环境权限执行。
8. 若无法完成 Windows 实机验证，准确记为“已实现，待实机验收”，不要标为全部完成。

## 2. 这次做什么、简化什么

### 必须保留的体验

- 真正位于普通桌面应用上方的悬浮球，黑绿主题。
- 球表达今日任务完成比例，以及计时运行/暂停状态。
- 拖动、左右靠边吸附、闲置缩起、悬停露出。
- 点击打开紧凑面板；支持“任务 / 专注”，任务内可切换“今日 / Todo”。
- 查看、新增、改标题、完成任务；选任务计时，也能自由计时。
- 开始、暂停、继续、结束保存；网站和浮窗使用相同数据与同一个计时状态。
- 托盘显示/隐藏、打开完整界面、退出；日常启动方便。

### 明确缩减的工程范围

| 原方案内容 | 个人版决定 |
| --- | --- |
| Electron + FastAPI + SQLite | 保留，不换前端框架，不引入 Rust |
| 小球和面板窗口 | 保留固定透明球 + 独立黑底面板；完整页面按需打开 |
| 大规模前端拆分 | 只抽重复的 API、同步和计时逻辑，现有网站 DOM 尽量保留 |
| 逐条任务接口、实体版本、多张操作表 | 首版改用现有 JSON 桶 + 桶版本检查，见第 3 节；不建完整命令框架 |
| change_log、SSE、同步协调器 | 不做；小数据量直接轮询现有数据，通常 2 秒一次 |
| 会话分段、精确跨日统计 | 不做；保留历史记录结构和按开始日期归档规则 |
| 自动离线同步、跨端草稿合并 | 不做；失败时保留当前输入并提示重试，不能显示保存成功 |
| 用户目录迁移与多来源导入向导 | 不做；继续使用 `backend/data.db`，改结构前做一次一致性备份 |
| 动态端口、复杂服务发现与认证体系 | 不做；使用固定 `127.0.0.1:8000` + 简单 health 身份检查 |
| 多用户、云同步、账号 | 不做 |
| 安装器、内置 Python、代码签名、自动更新 | 不做；使用本机现成 Python/Node 和启动脚本 |
| 开机启动、全局快捷键 | 暂不做；托盘和快捷方式够用后再看实际需求 |
| 完整跨平台/多屏认证矩阵 | 只验证自己当前 Windows 环境；保留工作区夹取与屏幕丢失回主屏 |
| 完整安全工程 | 保留回环监听、渲染器隔离、有限 IPC、避免静态暴露数据库这些低成本边界 |
| 性能监控和长时间压测 | 手动检查常驻占用和明显泄漏，不建监控系统 |

**轻量意味着减少工程层次和功能范围，不意味着牺牲黑绿风格、动效质量或允许静默丢数据。**

## 3. 固定实现约定

这些约定用于避免不同阶段的 AI 各自选择一套方案；具体函数、文件名可根据项目实际调整。

### 3.1 任务同步：用桶版本解决覆盖

继续保存每日任务数组与目标数组，不先迁成逐条数据库表。

- 每日任务桶按日期维护 revision；目标数组维护自己的 revision。版本可增加到现有表，或用一个小版本表实现。
- 读取接口返回数据和对应 revision；PUT 同时提供读取时的 `expectedRevision`。
- 后端在一个写事务内检查版本、替换数据并递增版本；不匹配返回 `409`，不写入。
- 网站和浮窗一起适配。旧的无版本请求明确要求刷新，不能继续覆盖。
- 发生冲突时拉最新数据，保留用户输入并提示重试。首版不自动合并，也不无限自动重放。
- 任务延续会修改两个日期，使用一个简单的事务接口完成，不能出现源日期已删除而目标日期保存失败。
- 同步轮询不能覆盖未提交的编辑框；只有服务端确认的数据才显示“已保存”。

桶版本可能使两端修改同日不同任务也发生冲突；个人自用频率下接受这个取舍。不要为了避免偶发重试升级成复杂同步系统。

### 3.2 计时：后端单例，保留简单记录

- 增加一条持久 `timer_state`，保存会话 ID、状态、关联任务、累计时间、运行起点和检查点等必要字段。
- 开始/暂停/继续/结束经后端处理；全应用最多一个活跃会话。前端每秒只更新显示。
- 保留原有时间戳校正思路；活进程时长优先用单调时钟，保存 UTC 时间用于重载展示。
- 开始、暂停、继续立即保存；运行中约每 5 秒保存检查点。后端异常重启恢复为暂停，不补算整个停机时间。
- 结束时在一个事务中写入现有 `study_sessions` 并清空活跃状态；使用同一个 session ID 防止重复保存，不另外创建通用 operations 表。
- 网页不再整天 PUT 学习记录；删除记录改为简单的单条删除接口，避免覆盖刚结束的会话。
- 保留旧记录 `{id, start, end, duration}`，新记录可附加任务引用与标题快照；按会话开始日期归档，不增加精确跨日图表。
- 显式结束时，大于零的时长可以保存；“放弃本次”才丢弃。不要继续把重置按钮同时承担保存和清空语义。
- 正在计时的任务不能无提示删除/完成。先暂停或结束该计时，再操作；首版不必做“保存并完成”的跨业务复合按钮。
- 更换任务先结束当前，再开始新任务；没有必要第一版就增加原子 switch 接口。

### 3.3 桌面：固定地址、简单常驻

- 桌面和网站使用同一个本地后端及数据库，前端 API 改为同源 `/api`。
- `GET /api/health` 返回应用标识和简单协议版本；8000 被其他服务占用时显示清楚的错误，不杀进程、不自动另起一套数据库。
- Electron 获取单实例锁；后端已是兼容 Daily Plan 服务时直接复用，否则启动本项目后端。
- 关闭完整窗口隐藏到托盘；面板关闭只是收起；真正退出时先保存暂停状态。只停止自己启动的后端。
- 当前阶段先在自己正在使用的显示器上做好体验；位置异常时放回主屏可见区。
- 原生窗口与业务状态分开：Electron 管窗口，FastAPI 管任务和计时。

## 4. 阶段总览

| 阶段 | 结果 | 依赖 | 当前状态 |
| --- | --- | --- | --- |
| S1 桌面骨架与窗口验证 | 桌面上可点开、可拖动的真实小球 | 无 | 已完成 |
| S2 共享数据与持久计时 | 网站和简单面板共用可靠数据 | S1 | 已实现待实机验收 |
| S3 紧凑任务与专注面板 | 在小面板内完成日常操作 | S2 | 未开始 |
| S4 停靠与状态动效 | 有完成进度和计时动效的靠边球 | S3 | 未开始 |
| S5 日常启动与收尾 | 自己能方便启动、常驻、恢复使用 | S4 | 未开始 |

每个阶段只围绕本阶段交付，不以“以后可能需要”为由扩建架构。

### 4.1 Astra / 5.6 分工与切换顺序

按用户指定的分工：**Astra 负责前端视觉、动效和交互；5.6 负责后端、数据、Electron 系统能力与运行收尾。** 这是本项目的工作安排，不依赖模型性能排名。

推荐主流程为 **5.6 → Astra → 5.6**，共两次主要切换。最后只有出现实际视觉问题时再交给 Astra 修整，无需机械增加一轮。

| 工作轮次 | 使用模型 | 执行范围 | 何时切换 |
| --- | --- | --- | --- |
| 第一轮：准备可接入的底座 | 5.6 | S1、S2；提前完成 S4 的原生停靠能力和窗口 IPC | 桌面窗口可运行，真实任务与计时接口可用，停靠/展开原生控制可调用，交接清楚后换 Astra |
| 第二轮：集中完成前端 | Astra | S3 全部；S4 的小球/面板视觉、交互状态机、动效及真实数据接入 | 真实桌面内完整使用链路和动效验收通过后换 5.6 |
| 第三轮：日常运行收尾 | 5.6 | S5；修复启动、托盘、休眠、退出、恢复和数据方面的问题 | 可方便日常使用后完成；不主动重做已验收 UI |
| 可选：视觉复验 | Astra | 仅处理联调后实际出现的布局、焦点、动效回归 | 没有问题则省略 |

S4 被拆为“原生能力”和“前端呈现”，原生部分提前到第一轮，S4 整体仍在第二轮验收后才标记完成。S1 的界面只是能验证窗口行为的骨架，5.6 不必精修外观。

**不要先用模拟接口做完全部精细前端，再开始 Electron 和后端。** 可以先做外观预览，但不能算前端最终完成：真实窗口的透明命中、焦点和屏幕边缘行为仍可能要求修改设计。本项目优先把底座做通，再让 Astra 连续完成前端，减少往返切换。

### 4.2 第一轮 5.6 需要提前准备的原生能力

除 S1/S2 外，从 S4 提前完成以下内容；只做可调用的行为，不做精细动画：

- 获取当前窗口/显示器工作区，拖动与位置夹取，左右吸附位置计算。
- 保存/恢复位置，屏幕配置变化后保证窗口可见。
- 显示、隐藏、定位面板；提供展开方向与球的锚点信息。
- 透明区域命中控制；提供自由球、贴边球、缩柄模式所需的有效交互区域切换。
- 允许前端报告开合动画完成，再隐藏窗口；避免主进程提前隐藏造成动画被截断。
- 提供少量明确的 preload 方法和事件。具体命名由实现确定，不暴露通用任意 IPC 或系统执行能力。

前端决定柄的样式、悬停延迟、开合节奏和反馈；主进程负责实际窗口位置、可见性与命中区域。不要在主进程和渲染器各写一套相互竞争的自动收起逻辑。

### 4.3 交给 Astra 前的最小交接

5.6 在本文第 10 节记录以下内容即可，不新增长篇设计文档：

1. **能运行：** 实际启动命令、前端入口、需要启动的后端、验证环境。
2. **能接数据：** 已实现的读取/写入/计时方法、返回字段、冲突与失败处理；指向实际共享模块或接口定义，必要时附一个脱敏示例。
3. **能控窗口：** preload 方法、事件、坐标单位、展开方向、动画完成通知与命中区域约定。
4. **明确归属：** Astra 可改的界面文件；已有网站 API 适配位置；仍有问题的后端/原生能力。
5. **验证结果：** S1/S2 验收结果，以及 S4 原生部分哪些已在真实 Windows 上测试。

接口满足当前功能即可，不必做 SDK、接口生成器或大型组件库。Astra 接手后直接使用真实数据；如发现少量接口缺口，可补最小适配并写明，避免为小问题反复切模型。涉及数据库语义或大幅原生改造时记录清楚交回 5.6。

### 4.4 各轮可直接使用的指令

**第一轮发给 5.6：**

> 按 `DESKTOP_IMPLEMENTATION_STAGES.md` 第 4.1–4.3 节执行第一轮：完成 S1、S2，并提前准备 S4 的原生停靠与窗口桥能力。前端先保留可验证功能的简洁骨架，精细视觉、动效和交互留给 Astra。使用真实接口验证数据与计时，完成后更新阶段状态和最小交接记录，不进入完整前端制作。

**第二轮切到 Astra：**

> 阅读阶段计划和 5.6 的交接，执行 S3 与 S4 前端部分。基于真实 Electron 窗口与现有接口，集中完成黑绿小球、任务/专注面板、交互、停靠呈现和状态动效。按需参考原产品方案，保持个人自用的精简范围。完成真实桌面验证，并记录交给 5.6 的收尾事项。

**第三轮切回 5.6：**

> 前端已经完成，请按阶段计划执行 S5 和交接中列出的工程问题，重点完成启动、托盘、休眠、退出与恢复。保留已经验收的视觉和交互，只做运行必需的接线与修复；有视觉回归时记录具体复现步骤，供 Astra 最后修整。

## 5. S1：桌面骨架与窗口验证

**先读：** 原方案第 2 节、第 5.3 节、第 8 节。第 8 节文件结构只作参考，不要求全部创建。

执行顺序：

1. 检查现有启动方式；添加最小 `package.json`、Electron 主进程、preload 和独立浮窗入口。
2. 先用模拟状态创建固定透明球和独立黑底面板，实现点击展开、收起、Esc、基础拖动。
3. 配置窗口置顶、隐藏任务栏图标与不抢焦点显示；验证球和面板的切换。
4. 处理透明空白处的鼠标命中，确保底层应用能点击、球能重新响应。不要使用铺满桌面的透明窗口。
5. 加最小托盘“显示 / 隐藏 / 退出”，避免测试时无法关闭常驻窗口。
6. 能按需打开当前网站；后端可暂由现有命令启动，自动管理放到 S5。

验收：

- 在其他常规应用上方能看到真实球；单击展开，拖动不会误触展开。
- 收起后没有残留大块透明区域阻挡其他应用；重新点击仍可操作。
- 托盘退出后 Electron 进程正常结束。

**本阶段不做：** 真实任务写入、完整计时业务、精细动效、安装包。

交接时记录实际启动命令、窗口方案是否通过实机验证，以及未解决的平台问题。模拟数据必须明确标识，并在 S3 删除。

## 6. S2：共享数据与持久计时

**先读：** 本文第 3 节；原方案第 8.2、9.5、10.1 节。原方案第 9 节的完整表和接口清单不照搬。

执行顺序：

1. 修改真实数据库结构前做一致性备份；测试使用临时数据库，不拿自己的正式任务试破坏性场景。
2. 按第 3.1 节给任务和目标数组添加版本保护，并把网站现有写路径一起改完，包括子任务操作和任务延续。
3. 抽取少量共享 API/数据读取逻辑，让网站和浮窗读同一个后端；实现简单轮询和编辑草稿保护。
4. 按第 3.2 节实现后端单例计时、检查点和会话保存；网站原计时按钮接入新接口。
5. 用 S1 的简单面板显示真实任务数量、当前计时和开始/暂停按钮，先证明共享链路正确。
6. 请求失败要可见；不会因 localStorage 兜底而误报已保存，不自动回写旧缓存覆盖新版数据。

验收：

- 网站修改任务后，面板约 2 秒内更新；两端同时写同一桶时不会静默覆盖。
- 开始计时后刷新网站，仍显示同一会话；暂停后时间不增长。
- 重复提交结束不会产生两条记录；服务重启后恢复为暂停并保留最近检查点。
- 旧任务、目标、子任务和学习记录仍可读取；任务延续不丢任务。

必要测试只覆盖：版本冲突、跨日期延续事务、暂停恢复、结束去重、后端重启恢复。不要建设完整测试平台。

**本阶段不做：** change_log、SSE、离线队列、逐条任务表、精确跨日分段、数据迁移向导。

## 7. S3：紧凑任务与专注面板

**先读：** 原方案第 6 节；以本文的精简范围覆盖其中更复杂的动作。

执行顺序：

1. 面板实现“任务 / 专注”；任务下实现“今日 / Todo”，明确它们来自两套现有数据。
2. 今日页支持新增、改标题、完成/取消完成、开始计时；显示标签、时间段和子任务完成数。
3. Todo 页支持同样的基本操作，保留现有 Todo 排序；不把 Goal 混进来。
4. 专注页支持选择任务或自由专注、开始、暂停、继续、结束保存；“结束并记录”调用 `/api/timer/finish`，“放弃本次”调用 `/api/timer/discard`，后者不写每日记录。运行中的任务操作按第 3.2 节保护。
5. 顶部实现固定面板、打开完整界面、收起。非固定面板失焦收起；编辑稿保留，重新展开可继续。
6. 加入无任务、保存失败、连接中断等简短状态；所有模拟任务和假计时移除。

验收：

- 从小球打开面板，新增任务、启动计时、暂停、结束保存，网站能核对到结果。
- 今日和 Todo 不串数据，今日完成环的统计不掺入 Todo。
- 中文输入 Enter 行为正常；轮询不会清空输入或改变光标。
- 任务页切专注页不会丢当前计时；结束后记录只出现一次。

**本阶段不做：** 小面板内完整时间拨盘、子任务增删重排、任务删除及撤销体系、Todo 加入今日、历史浏览和热力图。复杂编辑继续到网站完成。

## 8. S4：停靠与数据驱动动效

**先读：** 原方案第 4 节、第 5.1–5.2 节、第 7.1–7.2 节。直接按其中状态和初始尺寸实施，再按自己屏幕调整。

执行顺序：

1. 提取共享颜色 token，保持现有黑绿主题；避免把网站 body 布局套到透明球上。
2. 实现球外环的今日完成率；运行时中心显示紧凑时长，内环表达分钟节奏；暂停时冻结。
3. 实现左右边缘吸附、离开后缩柄、悬停露出、点击展开；拖动和输入期间不自动缩起。
4. 实现面板朝屏幕内侧展开，位置夹取到 workArea；保存停靠方向和相对位置。
5. 加任务完成反馈、面板开合、标签切换和按钮反馈；快速反向操作能取消旧动画。
6. 隐藏面板时停其动画；增加减少动效支持。完善文字、留白、焦点和可点击区域。

验收：

- 一眼区分未计时、运行、暂停、今日全完成和断连；外环比例来自真实数据。
- 连续展开/收起、拖动/吸附不会留下残影或失去点击能力。
- 靠左右边缘均不裁掉面板按钮；重启后球能恢复到可见位置。
- 动效流畅、克制，静置时没有高占用；减少动效时功能仍完整。

**本阶段不做：** 粒子系统、WebGL、可视化编辑器、多个皮肤、专注微时间轴。

## 9. S5：个人日常使用收尾

**先读：** 原方案第 11.2 节与第 13 节；测试只选与本阶段和当前电脑有关的场景。不要执行原方案完整安装分发工程。

执行顺序：

1. 按第 3.3 节实现 Electron 单实例、固定地址 health 检查、后端复用或隐藏启动，明确进程归属。
2. 加完整托盘菜单：显示/隐藏球、打开完整界面、暂停/继续、退出；关闭主窗口只隐藏。
3. 正常退出先暂停；锁屏/休眠自动暂停，恢复后手动继续；异常重启沿用检查点恢复。
4. 完善后端不可用提示和手动重试，不建复杂重启调度系统。
5. 提供本机可用的启动命令和双击脚本；复用现有 Python 环境，写清首次依赖安装步骤。脚本从自身路径启动，处理中文和空格路径。
6. 确认后端仅回环监听、有限 IPC、数据库不被静态目录直接暴露；不用为此增加账号系统。
7. 跑一轮完整日常链路和下方最小回归；修复实际问题后更新 README 的个人版使用方法。

最小回归：

- 双击启动两次，只存在一套桌面实例；8000 为其他程序时有可理解提示。
- 主窗口关闭后球仍可用；退出后自有后端清理，复用的独立后端不被误杀。
- 球与网站同时操作不静默覆盖；后端断开时不能误报保存成功。
- 锁屏、休眠恢复、网页刷新、后端重启后的计时行为与本文一致。
- 当前电脑的屏幕缩放下可读可点，重新启动不会把球放到看不见的位置。
- 正常使用一段时间，检查明显卡顿、异常常驻 CPU、重复进程和计时记录错误。

**本阶段不做：** 安装器、自动更新、开机启动、签名、多平台发布。完成后先实际使用，有具体不便再开下一轮。

## 10. 阶段交接记录

每阶段完成或中断后在这里追加一条，保持简短，帮助下一轮 AI 直接继续。不要另外创建多套状态文件。

```text
日期 / 阶段：
状态：未开始 / 进行中 / 已实现待实机验收 / 已完成
实际改动：文件 + 功能，最多 5 条
启动或验证命令：
验证结果：通过项；未验证项及原因
已知问题：仅记录会影响下一阶段的问题
下一步：具体到下一项动作
```

初始记录：2026-09-10，仅创建执行计划，S1–S5 均未实施。

日期 / 阶段：2026-09-10 / S1
状态：已完成
实际改动：新增 Electron 主进程、受限 preload、透明悬浮球、独立面板、托盘与渲染 smoke；修复第二次点击无响应和拖动误触。
启动或验证命令：`npm.cmd run desktop`；`npm.cmd run desktop:smoke`
验证结果：自动渲染 smoke 通过；用户实机确认展开、收起和交互正常。
已知问题：精细视觉、吸附和缩柄属于 S3/S4。
下一步：完成 S2 的共享数据与计时状态。

日期 / 阶段：2026-09-10 / S2
状态：已实现待实机验收
实际改动：`backend/main.py` 增加任务/目标版本保护、事务延续和持久计时；`app.js` 接入版本写入、轮询与后端计时；`shared/api.js` 提供共用接口；`floating/` 简单面板与球显示真实任务/计时；启动脚本仅监听回环地址。
启动或验证命令：后端执行 `python -m uvicorn main:app --host 127.0.0.1 --port 8000`，项目根目录执行 `npm.cmd run desktop`；自动测试执行 `python -m pytest tests\test_s2_backend.py -q`。
验证结果：11 项后端测试通过，覆盖全部公开数据接口、版本冲突、跨日期事务、计时状态机、结束去重、放弃与记录删除、重启恢复、首次迁移保护、静态文件隔离和 Electron CORS；JavaScript 语法与 `git diff --check` 通过。真实数据库修改前已备份到忽略提交的 `backend/backups/`。接口交接见 `DESKTOP_BACKEND_HANDOFF.md`。
已知问题：S2 面板只提供新增、选择任务开始计时、自由开始和暂停/继续；完成、改名、结束保存和精细反馈属于 S3。S4 原生吸附/缩柄桥尚未实施。
下一步：先按 S2 验收项完成实机联动；通过后由 5.6 补齐 S4 原生窗口能力，再交给 Astra 做 S3/S4 前端。

S2 修复补记：网页轮询原先把保持焦点的空新增框当作编辑草稿，导致浮窗写入后网页不接收新 revision，下一次网页保存触发 409。现改为只有真实任务/目标编辑稿才暂停对应数据桶刷新，并为 `app.js` 增加缓存版本标记；同时恢复 `file://` 打开时的本地 API 地址兼容。

入口修复补记：使用真实 `http://localhost:8000` 页面提交诊断任务成功，浮窗收到同一数据，随后已清理诊断任务。桌面快捷方式现在用带变化参数的 URL 打开，Electron 右上角再次打开完整界面时执行忽略缓存刷新，避免复用仍运行旧保存协议的标签页或主窗口。

浮窗响应优化补记：新增、完成/恢复、开始计时、暂停和继续改为先更新本地界面再由后端确认；成功时直接接收新 revision 或计时快照，不再额外拉取整份数据，失败时才重新加载回滚。网站与其他窗口仍通过轮询取得最终一致状态。

后端审计补记：修复迁移接口可被直接调用覆盖已有数据、项目根目录静态挂载暴露数据库/文档、同日延续与非法 revision 未明确拒绝、finish 返回 timer 字段不完整、启动脚本将任意 8000 监听误判为 Daily Plan。修复后真实服务复验通过，完整接口和前端约束集中记录在 `DESKTOP_BACKEND_HANDOFF.md`。

计时校准补记：修复两秒轮询用整数秒反复重置本地计时锚点造成的停顿/闪跳。同一运行会话只接受向前的服务器校准并保留本地亚秒锚点；网站也复用现有 interval，不再随每次轮询销毁重建。浮窗任务列表仅在对应 revision 变化时重绘，避免周期性丢失悬停、点击与滚动状态。

## 11. 给后续 AI 的启动指令

可以直接使用：

> 请按 `DESKTOP_IMPLEMENTATION_STAGES.md` 执行下一个未完成阶段。先检查实际代码和阶段交接记录，再按需阅读 `DESKTOP_FLOATING_ORB_PLAN.md` 对应章节。产品仅个人自用，以阶段计划中的精简范围为准，不扩建架构。完成代码、必要验证和交接记录；不要只提供建议，也不要把未做的实机验收写成通过。

全部阶段完成的标准：**我可以方便地启动它，在其他应用上方通过有动效的小球查看进度、靠边收起、打开任务、开始和结束计时，并在原网站看到同一份结果。**

### S3 / S4 前端实现补记（2026-09-11）

已实现黑绿悬浮球、任务/专注面板、编辑/完成/计时结束保存、确认放弃、即时反馈、轮询竞态保护与亚秒计时。补齐实际动效需要的原生吸附、缩柄、主进程命中恢复、位置保存、失焦收起和可反转展开动画。

详细范围、文件职责、验证结果与限制见 `DESKTOP_FRONTEND_HANDOFF.md`，后续 AI 先读该文件。本轮后端 11 项、时钟 2 项、Electron 隔离交互和原生开关/吸附检查通过；真实鼠标手感与多屏高 DPI 等待用户体验。S5 尚未实施，不应把本轮标记为全部桌面工程完成。
